import React from "react";

const ScheduledMeetings = ({ capsuleId }) => {
  return <div>Scheduled meetings for capsule ID: {capsuleId} will be displayed here.</div>;
};

export default ScheduledMeetings;
